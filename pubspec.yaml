name: quill_ai
description: Quill.AI — The Academic Companion
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  smooth_page_indicator: ^1.1.0
  http: ^1.2.0
  image_picker: ^1.1.2
  shared_preferences: ^2.2.3
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0
  supabase_flutter: ^2.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true