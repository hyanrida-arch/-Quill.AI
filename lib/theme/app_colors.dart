// lib/theme/app_colors.dart
import 'package:flutter/material.dart';

/// Quill.AI brand colors — Use these constants everywhere.
class AppColors {
  // Backgrounds
  static const Color white = Color(0xFFFFFFFF);
  static const Color subtleGray = Color(0xFFF7F8F9);
  static const Color lightTeal = Color(0xFFF0FDFA);

  // Text
  static const Color deepNavy = Color(0xFF0D1117);
  static const Color slateGray = Color(0xFF6B7280);

  // Accent (Mystro / AI only)
  static const Color teal = Color(0xFF14B8A6);

  // Borders
  static const Color border = Color(0xFFE5E7EB);

  // States
  static const Color red = Color(0xFFDC2626);
  static const Color amber = Color(0xFFF59E0B);

  // For overdue background
  static const Color redBackground = Color(0xFFFEF2F2);
  static const Color amberBackground = Color(0xFFFFFBEB);

  // --- Pomodoro & Dashboard Colors ---
  static const Color orange = Color(0xFFF97316);         // Orange
  static const Color pomodoroActive = Color(0xFFEF4444); // Red 500
  static const Color shortBreak = Color(0xFF10B981);     // Emerald 500
  static const Color longBreak = Color(0xFF3B82F6);      // Blue 500
}